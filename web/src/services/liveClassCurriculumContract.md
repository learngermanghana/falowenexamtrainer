# Live class curriculum compatibility

The student client reads `assignmentIds` first and falls back to `chapterIds`, `curriculumIds`, and legacy `assignment_id`. IDs are normalized to canonical uppercase values so sessions generated by Falowen Admin display the correct curriculum links.
