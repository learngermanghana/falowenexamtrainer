{
  "name": "falowen-exam-coach-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.13.2",
    "firebase": "^12.0.0",
    "i18next": "25.2.1",
    "jspdf": "^3.0.4",
    "react": "^19.2.1",
    "react-dom": "^19.2.1",
    "react-i18next": "^15.6.1",
    "react-router-dom": "^7.6.0",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "typescript": "4.9.5",
    "@babel/preset-env": "^7.28.5",
    "@babel/preset-react": "^7.28.5",
    "babel-jest": "^30.2.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "vite": "^7.2.7"
  },
  "overrides": {
    "typescript": "4.9.5"
  },
  "scripts": {
    "sync:classes": "node scripts/sync-class-brochure-data.mjs",
    "sync:goethe-config-ui": "node scripts/patch-goethe-exam-config-ui.mjs",
    "sync:live-class-session-identity": "node scripts/patch-live-class-session-identity.mjs",
    "sync:speaking-audio": "node ../scripts/runSpeakingAudioReliabilityPatch.mjs",
    "sync:sheet-result-feedback": "node ../scripts/patchSheetResultFeedbackPresentation.mjs",
    "sync:course-media": "node ../scripts/patchRequestedCourseMedia.mjs",
    "sync:b2-days-9-12": "node ../scripts/patchB2Days9To12ContentUpgrade.mjs",
    "sync:c1-opinion-template": "node ../scripts/patchC1OpinionEssayTemplate.mjs",
    "audit:a1-curriculum": "node ../scripts/auditA1CourseBook.js",
    "prestart": "npm run sync:speaking-audio && npm run sync:sheet-result-feedback && npm run sync:course-media && npm run sync:b2-days-9-12 && npm run sync:c1-opinion-template && npm run sync:live-class-session-identity && npm run audit:a1-curriculum && npm run sync:classes && npm run sync:goethe-config-ui",
    "prebuild": "npm run sync:speaking-audio && npm run sync:sheet-result-feedback && npm run sync:course-media && npm run sync:b2-days-9-12 && npm run sync:c1-opinion-template && npm run sync:live-class-session-identity && npm run audit:a1-curriculum && npm run sync:classes && npm run sync:goethe-config-ui",
    "pretest": "npm run sync:sheet-result-feedback && npm run sync:c1-opinion-template",
    "pretest:ci": "npm run sync:sheet-result-feedback && npm run sync:c1-opinion-template",
    "start": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "jest",
    "test:ci": "jest --runInBand"
  }
}
