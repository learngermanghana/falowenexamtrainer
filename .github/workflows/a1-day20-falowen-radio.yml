name: A1 Day 20 Falowen Radio

on:
  pull_request:
    paths:
      - "web/src/data/a1RadioResources.js"
      - "web/src/data/a1Day20FalowenRadio.test.js"
      - ".github/workflows/a1-day20-falowen-radio.yml"

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install web dependencies
        working-directory: web
        run: npm install --legacy-peer-deps --no-audit --no-fund
      - name: Test the A1 Day 20 Radio replacement
        working-directory: web
        env:
          CI: true
        run: npm run test:ci -- --runTestsByPath src/data/a1Day20FalowenRadio.test.js
      - name: Build production web app
        working-directory: web
        run: npm run build
