name: Apply course book task split

on:
  push:
    branches:
      - fix/course-book-task-cards-submit-tabs

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/course-book-task-cards-submit-tabs
          fetch-depth: 0

      - name: Apply course book and submission patch
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              file_path = Path(path)
              text = file_path.read_text()
              if old not in text:
                  raise SystemExit(f"Expected text not found in {path}: {old[:120]!r}")
              file_path.write_text(text.replace(old, new, 1))

          utility = '''const toArray = (value) => (Array.isArray(value) ? value : value ? [value] : []);

const TASK_SECTIONS = ["schreiben_sprechen", "lesen_hören"];

const normalizeToken = (value) =>
  String(value || "")
    .trim()
    .toLowerCase()
    .replace(/_/g, "-")
    .replace(/\s+/g, "");

const getTaskChapter = (task = {}, entry = {}) =>
  String(task.chapter || task.displayChapter || entry.displayChapter || entry.chapter || "").trim();

const getTaskAssignmentId = (task = {}, entry = {}) =>
  task.assignmentId || task.assignment_id || task.assignmentKey || entry.assignmentId || entry.assignment_id || entry.assignmentKey || null;

const getTaskTitle = (task = {}, entry = {}) =>
  task.topic || task.title || task.assignmentTitle || entry.topic || entry.title || entry.assignmentTitle || `Day ${entry.day}`;

export const expandCourseBookEntry = (entry = {}) => {
  const tasks = TASK_SECTIONS.flatMap((section) =>
    toArray(entry?.[section]).filter(Boolean).map((task) => ({ section, task }))
  );

  if (tasks.length <= 1) return [entry];

  return tasks.map(({ section, task }, index) => {
    const chapter = getTaskChapter(task, entry);
    const assignmentId = getTaskAssignmentId(task, entry);
    const assignment = task.assignment === undefined ? Boolean(entry.assignment) : Boolean(task.assignment);

    return {
      ...entry,
      topic: getTaskTitle(task, entry),
      title: task.title || task.topic || entry.title,
      chapter,
      displayChapter: chapter,
      assignment,
      progressionEligible:
        task.progressionEligible === undefined ? entry.progressionEligible : task.progressionEligible,
      assignmentId,
      assignment_id: assignmentId,
      lesen_hören: section === "lesen_hören" ? task : undefined,
      schreiben_sprechen: section === "schreiben_sprechen" ? task : undefined,
      courseBookTaskIndex: index + 1,
      courseBookTaskSection: section,
    };
  });
};

export const expandCourseBookEntries = (entries = []) =>
  toArray(entries).flatMap((entry) => expandCourseBookEntry(entry));

export const findCourseBookEntry = ({ entries = [], day, chapter = "" } = {}) => {
  const requestedDay = Number(day);
  const requestedChapter = normalizeToken(chapter);
  const matches = expandCourseBookEntries(entries).filter(
    (entry) => Number(entry?.displayDay ?? entry?.day) === requestedDay
  );

  if (!requestedChapter) return matches[0] || null;

  return (
    matches.find((entry) => {
      const tokens = [
        entry?.displayChapter,
        entry?.chapter,
        entry?.assignmentId,
        entry?.assignment_id,
        entry?.assignmentKey,
      ].map(normalizeToken);
      return tokens.includes(requestedChapter) || tokens.some((token) => token.endsWith(`-${requestedChapter}`));
    }) ||
    matches[0] ||
    null
  );
};
'''
          Path('web/src/utils/courseBookEntries.js').write_text(utility)

          utility_test = '''import { expandCourseBookEntries, findCourseBookEntry } from "./courseBookEntries";

describe("course book task entries", () => {
  const combinedDay = {
    day: 2,
    topic: "German Alphabet + Personal Pronouns",
    chapter: "0.2_1.1",
    lesen_hören: [
      { chapter: "0.2", title: "German Alphabet", assignment: true, assignmentId: "A1-0.2" },
      { chapter: "1.1", title: "Personal Pronouns", assignment: true, assignmentId: "A1-1.1" },
    ],
  };

  it("creates one course-book card per task on a multi-task day", () => {
    const entries = expandCourseBookEntries([combinedDay]);

    expect(entries).toHaveLength(2);
    expect(entries.map((entry) => entry.chapter)).toEqual(["0.2", "1.1"]);
    expect(entries.map((entry) => entry.topic)).toEqual(["German Alphabet", "Personal Pronouns"]);
    expect(entries.every((entry) => entry.day === 2)).toBe(true);
    expect(entries[0].lesen_hören.chapter).toBe("0.2");
    expect(entries[1].lesen_hören.chapter).toBe("1.1");
  });

  it("keeps a single-task day as one normal day card", () => {
    const day4 = { day: 4, topic: "Numbers", chapter: "2", assignment: true };
    const entries = expandCourseBookEntries([day4]);

    expect(entries).toHaveLength(1);
    expect(entries[0]).toBe(day4);
  });

  it("finds the correct task after opening or refreshing a chapter-specific lesson URL", () => {
    expect(findCourseBookEntry({ entries: [combinedDay], day: 2, chapter: "1.1" })).toEqual(
      expect.objectContaining({ chapter: "1.1", topic: "Personal Pronouns" })
    );
    expect(findCourseBookEntry({ entries: [combinedDay], day: 2, chapter: "A1-0.2" })).toEqual(
      expect.objectContaining({ chapter: "0.2", topic: "German Alphabet" })
    );
  });
});
'''
          Path('web/src/utils/courseBookEntries.test.js').write_text(utility_test)

          replace_once(
              'web/src/components/CourseTab.js',
              'import { resolveAssignmentCanonicalKey } from "../utils/assignmentIdentity";\n',
              'import { resolveAssignmentCanonicalKey } from "../utils/assignmentIdentity";\nimport { expandCourseBookEntries } from "../utils/courseBookEntries";\n'
          )

          replace_once(
              'web/src/components/CourseTab.js',
              '''  const schedule = useMemo(() => {
    const seenByDay = {};
    return sortByDay(
      (schedules[selectedCourseLevel] || []).map((entry) => {
        const dayKey = String(getCourseBookDisplayDay(entry) || "");
        seenByDay[dayKey] = (seenByDay[dayKey] || 0) + 1;
        return { ...entry, occurrence: seenByDay[dayKey] };
      })
    );
  }, [schedules, selectedCourseLevel]);''',
              '''  const schedule = useMemo(() => {
    const seenByDay = {};
    const expandedEntries = expandCourseBookEntries(schedules[selectedCourseLevel] || []);
    return sortByDay(
      expandedEntries.map((entry) => {
        const dayKey = String(getCourseBookDisplayDay(entry) || "");
        seenByDay[dayKey] = (seenByDay[dayKey] || 0) + 1;
        return { ...entry, occurrence: seenByDay[dayKey] };
      })
    );
  }, [schedules, selectedCourseLevel]);'''
          )

          replace_once(
              'web/src/components/CourseTab.js',
              '''  const openLesson = (entry) => {
    navigate(`/campus/course/lesson/${selectedCourseLevel}/${entry.day}`, {
      state: {
        level: selectedCourseLevel,
        day: entry.day,
        entry,
        assignmentKey: entry.assignmentKey,
        status: entry.status,
        scoreText: entry.scoreBadge?.text || "",
      },
    });
  };''',
              '''  const openLesson = (entry) => {
    const chapter = String(entry.displayChapter || entry.chapter || "").trim();
    const search = chapter ? `?chapter=${encodeURIComponent(chapter)}` : "";
    navigate(`/campus/course/lesson/${selectedCourseLevel}/${entry.day}${search}`, {
      state: {
        level: selectedCourseLevel,
        day: entry.day,
        entry,
        assignmentKey: entry.assignmentKey,
        status: entry.status,
        scoreText: entry.scoreBadge?.text || "",
      },
    });
  };'''
          )

          replace_once(
              'web/src/components/CourseTab.js',
              '''const getEntryAssignmentId = (entry, level, occurrence = 1) => {
  if (!entry) return "";
  const normalizedLevel = normalizeLevel(level);''',
              '''const getEntryAssignmentId = (entry, level, occurrence = 1) => {
  if (!entry) return "";
  const normalizedLevel = normalizeLevel(level);
  if (entry?.courseBookTaskSection && !hasTutorMarkedWork(entry)) {
    const displayDay = getCourseBookDisplayDay(entry);
    const chapterToken = String(entry.displayChapter || entry.chapter || occurrence)
      .trim()
      .replace(/[^a-z0-9._-]/gi, "-");
    return `${normalizedLevel || "GENERAL"}-DAY-${displayDay}-PRACTICE-${chapterToken || occurrence}`;
  }'''
          )

          replace_once(
              'web/src/components/CourseLessonPageLegacy.js',
              'import { normalizeLesson } from "../data/lessonModel";\n',
              'import { normalizeLesson } from "../data/lessonModel";\nimport { findCourseBookEntry } from "../utils/courseBookEntries";\n'
          )

          replace_once(
              'web/src/components/CourseLessonPageLegacy.js',
              '''  const entry = useMemo(() => {
    if (location.state?.entry) return location.state.entry;
    return (
      (courseSchedules[level] || []).find(
        (lesson) => String(lesson.day) === String(day),
      ) || null
    );
  }, [day, level, location.state]);''',
              '''  const requestedChapter = useMemo(
    () => new URLSearchParams(location.search || "").get("chapter") || "",
    [location.search]
  );
  const entry = useMemo(() => {
    if (location.state?.entry) return location.state.entry;
    return findCourseBookEntry({
      entries: courseSchedules[level] || [],
      day,
      chapter: requestedChapter,
    });
  }, [day, level, location.state, requestedChapter]);'''
          )

          replace_once(
              'web/src/utils/courseLessonAssignments.js',
              '''  return (Array.isArray(entries) ? entries : []).reduce((assignments, entry, index) => {
    if (!entry?.assignment || entry?.progressionEligible === false || Number(entry?.assignmentDay) !== numericDay) {
      return assignments;
    }''',
              '''  return (Array.isArray(entries) ? entries : []).reduce((assignments, entry, index) => {
    const courseBookDay = Number(entry?.displayDay ?? entry?.assignmentDay ?? entry?.day);
    if (!entry?.assignment || entry?.progressionEligible === false || courseBookDay !== numericDay) {
      return assignments;
    }'''
          )

          replace_once(
              'web/src/components/CourseLessonInlineSubmission.test.js',
              'return /^(A1|A2|B1)-\\d+\\.\\d+$/.test(token) ? token : "";',
              'return /^(A1|A2|B1)-\\d+(?:\\.\\d+)?$/.test(token) ? token : "";'
          )

          replace_once(
              'web/src/components/CourseLessonInlineSubmission.test.js',
              '''  it("never returns null or invalid assignment keys and removes duplicate catalog entries", () => {''',
              '''  it("uses the visible course-book day when canonical entries have different internal day numbers", () => {
    const entries = [
      { assignmentDay: 2, displayDay: 2, topic: "Alphabet", assignment: true, chapter: "0.2", assignment_id: "A1-0.2" },
      { assignmentDay: 3, displayDay: 2, topic: "Pronouns", assignment: true, chapter: "1.1", assignment_id: "A1-1.1" },
      { assignmentDay: 6, displayDay: 4, topic: "Numbers", assignment: true, chapter: "2", assignment_id: "A1-2" },
    ];

    expect(buildAssignments("A1", 2, entries).map((assignment) => assignment.assignmentKey)).toEqual([
      "A1-0.2",
      "A1-1.1",
    ]);
    expect(buildAssignments("A1", 4, entries)).toEqual([
      expect.objectContaining({ assignmentKey: "A1-2", chapter: "2", day: 4 }),
    ]);
  });

  it("never returns null or invalid assignment keys and removes duplicate catalog entries", () => {'''
          )

          day4 = Path('web/src/components/A1Day4NumbersForBeginnersWorkbookPage.js')
          day4_text = day4.read_text()
          old_intro = 'Please complete all parts below and submit your final answers in the submission area, not on this page.'
          if old_intro not in day4_text:
              raise SystemExit('Expected A1 Day 4 introduction was not found')
          day4_text = day4_text.replace(
              old_intro,
              'Complete the assignment below, then open the Submit tab above to send your answers immediately.',
              1,
          )
          old_submission = 'Submit all your answers in the submission area after you complete the workbook tasks.'
          if old_submission not in day4_text:
              raise SystemExit('Expected A1 Day 4 submission text was not found')
          day4_text = day4_text.replace(
              old_submission,
              'After completing the workbook tasks, use the Submit tab above. The correct A1 Day 4 assignment is selected automatically.',
              1,
          )
          old_link = '''        <a
          href="/campus/course?submitWork=1"
          target="_blank"
          rel="noreferrer"
          style={{ ...styles.button, textDecoration: "none", width: "fit-content" }}
        >
          Go to Submission Area
        </a>'''
          if old_link not in day4_text:
              raise SystemExit('Expected A1 Day 4 legacy submission link was not found')
          day4_text = day4_text.replace(
              old_link,
              '''        <p style={{ margin: 0, fontWeight: 700, color: "#1d4ed8" }}>
          Choose Submit above when you are ready.
        </p>''',
              1,
          )
          day4.write_text(day4_text)

          Path('.github/workflows/apply-course-book-task-split.yml').unlink()
          PY

      - name: Commit patched source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Split course book task cards and standardize submit tabs"
          git push origin HEAD:fix/course-book-task-cards-submit-tabs

      - name: Run focused tests
        working-directory: web
        run: |
          npm ci
          CI=true npm test -- --watchAll=false --runInBand src/utils/courseBookEntries.test.js src/components/CourseLessonInlineSubmission.test.js
