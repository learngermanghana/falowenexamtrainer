{
  "buildCommand": "cd web && npm install && npm run build",
  "outputDirectory": "web/build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
