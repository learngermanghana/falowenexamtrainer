const fs = require('fs');
const path = require('path');

const repoRoot = path.resolve(__dirname, '..');
const generatedHeader = `// AUTO-GENERATED FILE.\n// Do not edit this file directly.\n// Edit shared/curriculumCanonical.json and run npm run sync:curriculum.\n\n`;

const webManifest = `${generatedHeader}import {\n  lessonCatalog,\n  CURRICULUM_ENTRIES as RAW_CURRICULUM_ENTRIES,\n  getCurriculumEntriesForLevel as getRawCurriculumEntriesForLevel,\n  normalizeLevel,\n} from "./lessonCatalog.js";\nimport { alignA1CurriculumEntries } from "./a1RouteAlignment.js";\nimport { alignB2CurriculumEntries } from "./b2LessonContentAlignment.js";\n\nconst alignRuntimeCurriculumEntries = (entries = []) =>\n  alignB2CurriculumEntries(alignA1CurriculumEntries(entries));\n\nconst CANONICAL_CURRICULUM = lessonCatalog;\nconst CURRICULUM_ENTRIES = alignRuntimeCurriculumEntries(RAW_CURRICULUM_ENTRIES);\nconst CURRICULUM_BY_LEVEL = CURRICULUM_ENTRIES.reduce((acc, entry) => {\n  if (!acc[entry.level]) acc[entry.level] = [];\n  acc[entry.level].push(entry);\n  return acc;\n}, {});\nconst getCurriculumEntriesForLevel = (level) =>\n  alignRuntimeCurriculumEntries(getRawCurriculumEntriesForLevel(level));\n\nexport {\n  CANONICAL_CURRICULUM,\n  CURRICULUM_ENTRIES,\n  CURRICULUM_BY_LEVEL,\n  getCurriculumEntriesForLevel,\n  normalizeLevel,\n};\n`;

const a1Cards = `${generatedHeader}import { getLessonsByLevel, getLessonDisplayData } from "./lessonCatalog.js";\nimport { applyA1LessonTitleOverride } from "./a1LessonTitleOverrides.js";\nimport { alignA1CurriculumEntries } from "./a1RouteAlignment.js";\n\nconst applyRequestedA1TitleCorrection = (lesson = {}) =>\n  String(lesson.id || lesson.assignmentId || "").trim().toUpperCase() === "A1-1.2"\n    ? applyA1LessonTitleOverride(lesson)\n    : lesson;\n\nconst ALIGNED_A1_LESSONS = alignA1CurriculumEntries(getLessonsByLevel("A1"))\n  .map((lesson) => applyRequestedA1TitleCorrection(lesson));\nconst A1_COURSE_BOOK_CARDS = Object.freeze(ALIGNED_A1_LESSONS.map((lesson) => Object.freeze({\n  lessonId: lesson.id,\n  ...getLessonDisplayData(lesson),\n  chapter: lesson.chapter,\n  title: lesson.title,\n  assessmentType: lesson.submissionRequired ? "tutor-marked" : "self-practice",\n  assignmentId: lesson.assignmentId,\n  resourceSection: lesson.kind || "lesen_hören",\n  submissionRequired: lesson.submissionRequired,\n  progressionEligible: lesson.progressionEligible,\n  ...(Object.prototype.hasOwnProperty.call(lesson, "grammarPage") ? { grammarPage: lesson.grammarPage || "" } : {}),\n  ...(Object.prototype.hasOwnProperty.call(lesson, "workbookRoute") ? { workbookRoute: lesson.workbookRoute || "" } : {}),\n})));\nconst normalizeA1Token=(value="")=>String(value||"").trim().toLowerCase().replace(/\\s+/g," ");\nconst getA1CourseBookCard=({displayDay,chapter,assignmentId,title}={})=>{\n  const day=Number(displayDay);\n  const normalizedChapter=String(chapter||"").trim();\n  const normalizedAssignmentId=String(assignmentId||"").trim().toUpperCase();\n  const normalizedTitle=normalizeA1Token(title);\n  const exact=A1_COURSE_BOOK_CARDS.find((card)=>Number(card.displayDay)===day&&card.chapter===normalizedChapter);\n  if(exact)return exact;\n  return A1_COURSE_BOOK_CARDS.find((card)=>normalizedAssignmentId&&String(card.assignmentId||"").trim().toUpperCase()===normalizedAssignmentId&&(!Number.isFinite(day)||Number(card.displayDay)===day)&&(!normalizedTitle||normalizeA1Token(card.title)===normalizedTitle))||null;\n};\nexport {A1_COURSE_BOOK_CARDS,getA1CourseBookCard};\n`;

fs.writeFileSync(
  path.join(repoRoot, 'web/src/data/curriculumManifest.js'),
  webManifest,
  'utf8',
);
fs.writeFileSync(
  path.join(repoRoot, 'web/src/data/a1CourseBookCards.js'),
  a1Cards,
  'utf8',
);

console.log('Applied centralized A1 route alignment to generated web curriculum files.');
